export interface LoginUserResponse {
  username: string;
  message: string;
  jwt: string;
  status: boolean;
}

export interface SignupUserResponse {
  username: string;
  message: string;
  jwt: string;
  status: boolean;
}
